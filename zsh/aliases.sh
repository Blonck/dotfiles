# local aliases
alias ls='ls --color=auto -F'
alias lsa='ls -al'
alias lsh='ls -ld .*'
alias f='find |grep'
alias ..='cd ..'
alias ...='cd ../..'

# auto extension
alias -s html=$BROWSER
alias -s txt=$EDITOR
alias -s tex=$EDITOR
alias -s h=$EDITOR
alias -s cpp=$EDITOR
alias -s hpp=$EDITOR
alias -s pdf=evince
alias -s ps=evince

# command L equivalent to command |less
alias -g L='|less'

